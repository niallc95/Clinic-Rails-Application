<%= simple_form_for([@patient, @patient.reports.build]) do |f| %>
	<%= f.input :date, :input_html => {:style => 'width: 250px;height:50px;'} %>
	<%= f.input :report, :input_html => {:style => 'height:320px;'}%>
	<center><%= f.button :submit, :class => 'button_one'  %></center>
<% end %>